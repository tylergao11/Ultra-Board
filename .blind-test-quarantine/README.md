# Post-event holdout

这里是后验人工标签的唯一隔离边界，不属于节点日可见信息，也不是行情原始数据。

旧 G3 研究、旧模型预测和旧派生结果已经删除；这里只允许保存当前人工明确锁定的干净标签。标签只能由显式 `backtest --labels` 读取，普通选层入口禁止访问。

日常节点取证只使用 `data/kaipanla/raw/` 中不晚于目标日期的证据，以及带同日 `information_cutoff` 的安全派生快照。
