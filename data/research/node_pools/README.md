# 节点记忆池快照

节点日期必须由人工在当时确认，文件名同时包含节点日和生成截止日：

```text
SEED_DATE__cutoff_CUTOFF_DATE.json
```

同一节点可以随着盲测推进生成多个截止版本。旧版本不得覆盖，因为它证明当时没有读取更晚结果。

本目录只保存 `tools/market_memory.py pool` 生成的事实快照，不保存核心、补涨、新题材、成功或失败等后验标签。
