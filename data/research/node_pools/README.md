# 人工日期范围事实快照

起始日期必须由人工指定，文件名同时包含起始日和生成截止日：

```text
SEED_DATE__cutoff_CUTOFF_DATE.json
```

同一起始日可以生成多个截止版本。旧版本不得覆盖，因为它证明当时没有读取更晚结果。

本目录只保存 `tools/market_memory.py pool` 生成的事实快照。目录名沿用历史路径；快照本身不确认该日是否构成节点，也不保存核心、补涨、新题材、成功或失败等结论。
