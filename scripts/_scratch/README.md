# 历史探针（非主路径）

此处为开发期 debug / probe 脚本，**不是**产品入口。

主链路请用仓库根 `README.md` 中的：

```bash
python -m ultraboard.kaipanla.backfill
python -m ultraboard.kaipanla.ohlc
python -m ultraboard.review.ladder_daily
```

新能力请加到 `ultraboard/`，不要往本目录堆正式功能。
