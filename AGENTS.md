# Ultra-Board

用户最新指示优先。

## 工作方式

- 轻量上阵，不做繁重的交付工程，除非用户要求。
- 禁止子Agent、新分支、测试工程和股票特例硬编码。
- 真相源统一，接口清晰；数据接口只提供来源事实。
- 讨论直接在对话中进行，不自行扩展成代码、统计或文档维护任务。

## 复盘执行顺序

1. 看日期：一次只推进一个交易日。
2. 看一字：从已有当日行情中筛出讨论对象。
3. 看属性消息面：只读这些对象对应的开盘啦消息及首板、二板起步记录，确定实际细分。
4. 查数量：归类确定后，只查对应细分的必要日期数量，完成当前节点判断。

- 这个顺序用于逐步缩小查询范围、加速判断，必须实际执行。禁止先收集整月资料、全量属性或所有板块数量再筛选。
- 足以排除的对象当场结束；当天得出判断就直接在对话中说明，再推进下一天。有缺口只保留具体缺项，不围绕它泛滥收集资料。

## 已下载数据直接使用

- 用户提前下载的本地原始数据、缓存和已有查询结果，是本轮讨论的事实基础，默认直接使用。用户没有要求重新核实，不得以“核实”“交叉验证”“更可靠”为由重复联网查数、重新下载或另找来源。
- 先通过现有数据入口读取所需日期和字段；不能跳过本地数据，直接搜索财联社、腾讯、打板客或其他外部网站。已有查询接口能读取缓存时，直接复用缓存。
- 比较竞价高低，先使用已有开盘涨幅，或已有开盘价与对应前收盘参考价。不得把“竞价过程快照文件为空”当成“没有开盘数据”，也不得因此另搜新闻补竞价。
- 只有本地确实缺少影响当前判断的数据，或已有数据出现具体矛盾时，才说明对应日期、对象和缺失字段，并按下方来源约束补这一项。不得仅凭某个目录或单个文件为空，认定全部已有数据不可用。
- 下方“核对原文”指先阅读已下载的开盘啦原文；允许使用外部行情来源，不代表需要重复核实已有行情。禁止把复盘讨论变成数据重采集或接口开发任务。

## 当前状态

2026-09-14，用户要求“删除当前逻辑，重来”。原有复盘规则、节点结论、持仓路径及方法评价全部停止沿用。

本轮重新确认的讨论口径见下文。旧规则、旧模型和旧结论不因本轮讨论重新生效，不主动从历史聊天或记忆中恢复；当前没有假定持仓。

原始行情、来源事实、查询接口和缓存保留；保留数据不代表保留旧判断。

## 来源约束

- 数据以“交易日＋股票代码”为中心。行情和催化分别使用各自唯一来源；成员关系按个股保存细分编号列表，细分成员与涨停交集在查询时生成。禁止按题材重复保存每只成员的完整行情或原始行。迁移清理必须先验证成员集合一致再删除旧文件。

- 2026-09-15 用户确认：本轮只处理数据；炸板名单及触板状态保留，无法取得的历史炸板首次触板时间允许留空，不再追补，不作为完成阻塞项。已取得的时间保留；封住涨停的首次封板时间仍需提供。完成后直接提交推送当前分支。

- 行情采用明确主次：已有同花顺行情直接使用，打板客只补缺项；开收盘价用同花顺未复权行情，普通交易日按前收盘价计算开盘涨幅，除权日补参考价。不因补缺源数值不同重拉、追查或专门记录差异。
- 属性、消息催化、板块异动解释和对应细分的涨停数量，只用开盘啦。这里是唯一来源，不是“优先”，不得用财联社、同花顺、打板客或其他网站的复盘、涨停原因替代。
- 封板、开板、回封时间、板型、价格等行情事实，直接使用已有数据；确有缺项时可以使用打板客补充。读取其行情不代表可以使用其属性分类或涨停解释。
- 判断前先核对开盘啦当日复盘中对应一字板的消息、板块异动和催化原文，结合首板、二板的起步记录，弄清消息实际推动的细分。不能用政策标题中的词或公司已有标签代替完整催化。
- 缺少对应的开盘啦原文时，明确缺口并继续查开盘啦；不得自行换源认定属性，也不得在催化未核清时给出确定的做或不做结论。

## 当前核心逻辑：根据消息，细分归类

- 根据开盘啦首板、二板当时的消息，结合启动节点和同批股票的炒作逻辑，认定实际起步属性。公司主营行业、主分类和附带标签只提供线索，不能代替开盘啦催化原文；漏标某个题材，不代表该股没有参与这轮题材炒作。
- 首板、二板都属于起步；三板及以上再新增或切换属性，属于用户不接受的“空中改属性”。不拿后来补上的标签回填起步逻辑。
- 手法是一字首开：找到核心，判断对应题材的发展是否把核心次日的预期推到“被迫继续一字”。没有被迫一字的预期时，首开后的回封是所讨论的机会；若继续一字才合理，首开回封不能按同一种机会处理。
- 不得擅自添加“整轮连板只认第一次开板”的限制。曾开板、随后重新一字的对象，不能仅凭此前开过板排除后续机会；仍按当时的催化、题材变化和压制关系判断。
- 顺序是：消息细分归类 → 找核心 → 比较对应题材的升级、持平或降级 → 判断次日是否被迫一字 → 判断首开回封是否符合预期。先归类，再数涨停；比较口径一致，股票自身计入对应题材的涨停数量。不能靠切出一个与实际炒作不符的小类别，制造“持平、可以做”的结论。
- 同一股票可以属于多个细分。选定催化对应的细分后，按开盘啦完整成员与当日涨停池的交集计数，不得因成员另有属性或催化而手动剔除，也不得先比较各属性数量再挑有利结果倒推催化。
- 只围绕一字首开对象讨论；足以排除的对象当场结束，只补影响判断的缺项，不扩展成无关股票或全量资料研究。
- 同一轮实际消息推动的股票，即使开盘啦展示的标签不同，也必须放在一起比较，不能机械拆开标签漏掉竞争对象。
- 前一日同属性更高身位一字构成压制，不能用次日高位股低开、低位股竞价更高的“反卡”说法撤销这项排除。
- 用户所说的竞价卡位，就是同属性、同身位或更高身位对象“竞价比它高”。不擅自改成封单金额比较，也不额外要求“自身明显走弱、核心地位被破坏”才承认卡位。不凭同地域标签任意建立压制关系，两只都涨停开盘时不把价格取整造成的涨幅差当成卡位。
- 并购重组公告驱动的一字票不做；不能拿收购标的或接盘方的行业、业务，改写成可参与的题材催化。
- 同一天多个对象符合条件，只做最先给出有效首开回封机会的一个。先排除不合格对象，再比较买点时间；不能用最终封板时间替代首次有效回封时间。
- 历史案例按当时能够看到的信息讨论。用户给出的数量、研究判断与独立核验的来源事实分别说明；不把案例判断写成股票特例或自动交易规则，不用后续输赢倒推、改写当时归类和预期。

## 数据入口

- 接口、字段和已知限制：[数据基建](docs/数据基建.md)。其中历史交易讨论的描述不构成当前规则。
- 本地行情确有缺项时的快速补充入口：[打板客每日页](https://dabanke.com/index-20250306.html)，按日期替换网址中的 YYYYMMDD；只补当前对象缺失的行情事实，不取属性、消息催化或细分涨停数量。
- 当前接续状态：[节点接力复盘接续](docs/节点接力复盘接续.md)。

## AI 复盘数据接口

- 目标是准确、便捷、无冗余，默认紧凑 JSON；按日期、股票代码、股票池和字段筛选，不展开无关信息。
- 正式离线入口为 `tools/replay_query.py` 的 `query()`；存储文件可能是引用或压缩结构，禁止绕过读取接口把引用文件当成缺数据。
- 个股日价格只存 `data/stock_prices/年份.sqlite3`；年行情和每日价格文件只作映射。成员关系只存个股到细分编号，数量实时取交集。
- `null` 表示缺失，不得冒充零、否或空列表；首次触板时间不得以最终封板时间代替。
- 催化组解释只返回一次，个股引用组编号；来源事实与派生展示不得重复落盘。
