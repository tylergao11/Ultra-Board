# Ultra-Board

用户最新指示优先。本文件维护长期适用的工作约定；任务进度和覆盖情况读取各自记录。

## 工作方式

- 由主 Agent 在现有分支直接完成任务，采用轻量、通用的实现和针对改动的必要检查。
- 复盘讨论在对话中逐个节点推进，围绕影响判断的对象和字段取数，得出结论后进入下一节点。
- 数据面向 AI 使用，以准确、便捷、无冗余为目标，按需返回紧凑 JSON。
- 方法以本文件及用户明确确认的口径为准；历史记录作为事实材料使用，持仓以用户明确提供的信息为准。

## 复盘顺序

1. 看日期：确定一个交易日，读取本地数据。
2. 看一字：查询一字板的代码、名称、身位和必要行情，筛出讨论对象。
3. 看属性消息面：读取对象的开盘啦催化及首板、二板起步记录，确定实际细分。
4. 查数量：按已确定的细分，查询必要日期的涨停交集数量。

每一步都用于缩小下一步的查询范围。足以排除的对象当场结束，有缺口时明确日期、对象、字段及其对判断的影响。

## 本地查询

统一入口为 `tools/replay_query.py` 的 `query()` 或同名命令行。直接使用已入库事实；“核对消息”“开盘啦原文”均指读取本地保存的来源内容。数据更新由下方维护入口负责。

```python
from tools.replay_query import query

# day 为交易日；code、plate 为已确定的股票代码和细分编号。
query(day, pool="one-price", fields=("code", "name", "boards", "board_type"))
query(day, code=code, fields=("code", "catalysts", "plate_ids"))
query(day, plate=plate, pool="limit", fields=("code",))
query(day, code=code, fields=("code", "open_pct", "first_limit_time", "has_reseal"))
```

- 竞价比较使用 `open_pct`；需要解释计算依据时读取本地开盘价和参考价。
- `plate.limit_count` 是完整细分成员与同日开盘啦涨停池的交集数量；`count` 是本次返回股票条数。
- 细分需求同时来自栏目编号与催化标题中的实际细分名称，不能只采大类。维护脚本按开盘啦名称编号缓存匹配并采集前后交易日；名称未解析、完整成员未齐备均为必需缺口。旧版覆盖检查不能证明新版细分需求已齐备。
- 个股催化的 `group` 引用顶层 `catalyst_groups`，结合个股原文与对应组解释理解消息。
- 保持 `null` 的未知含义，通过 `missing_components` 识别组件缺失；交易日由本地日历确定。
- 分别理解首次触板、最后封板和首次有效回封。`has_reseal` 根据开板次数及收盘状态推导，仅表示有无回封。
- 炸板首次触板时间允许为空；保留名单、触板状态及已取得时间，时序判断只使用有明确时间的记录。
- 历史成员关系按供应商补采版本理解，结合采集时间区分补采记录与交易当日快照。
- 覆盖范围和缺口读取 `data/replay/coverage.json`。查询结果缺少所需字段时，按[数据基建](docs/数据基建.md)定位对应本地记录。
- 底层映射和压缩结构使用 `ultraboard.kaipanla.query._read` 解析；据完整读取结果判断数据是否存在。

## 来源与存储

| 内容 | 事实来源与组织方式 |
| --- | --- |
| 属性、催化、板块异动解释、细分成员及涨停交集 | 开盘啦唯一来源 |
| 行情 | 已保存的同花顺主源与打板客补缺结果 |
| 开收盘价及开盘涨幅 | 同花顺未复权行情与对应参考价 |
| 股票日价格 | `data/stock_prices/年份.sqlite3`，以股票代码和交易日为唯一键；其他入口使用映射 |
| 成员关系 | 个股到细分编号的映射，交集数量在查询时计算 |
| 催化组解释 | 保存或返回一次，个股通过组编号引用 |

每项事实保留唯一记录，派生数量、时间格式和状态在读取时生成。迁移时先回读验证，再清除旧副本；复盘直接采用已确定的来源事实。

## 每日数据更新

用户要求新增或补齐数据时，直接调用：

```bash
python tools/update_data.py
```

也可使用 `npm run data:update`。

- 默认按本地交易日历处理新增日期、必需缺口和必要前日上下文；北京时间16点后纳入当天。
- `--check` 只输出计划；`--start YYYY-MM-DD --end YYYY-MM-DD` 指定历史范围。
- 由脚本完成采集、入库、补缺和最终检查；失败后重跑同一入口续补，已完整的日期直接跳过。
- `up_to_date` 表示已完整，`complete` 表示本次完成，`needs_attention` 且退出码2表示仍有必需缺项。
- 覆盖报告读取 `data/replay/coverage.json`；运行状态和详细日志分别读取 `data/replay/complete_state.json`、`data/replay/complete.log`。
- 允许为空的炸板首次触板时间保持为空。日历缺失时报告具体年份，由数据维护补齐。
- 维护完成后，按用户已有授权提交推送现有分支。

## 复盘口径：根据消息，细分归类

- 从开盘啦首板、二板当时的消息、启动节点和同批股票的炒作逻辑认定起步属性。主营、主分类和附带标签用于提供线索；实际催化决定归类，漏标也可能参与同轮炒作。
- 首板、二板均属于起步；起步属性以这两个阶段的消息为依据。三板及以上新增或切换属性按“空中改属性”识别，起步逻辑仍保留当时的口径。
- 手法是一字首开：先找到核心，判断题材发展是否把次日预期推到“被迫继续一字”。预期允许开板时，讨论首开后的回封机会；预期要求继续一字时，按该预期重新判断首开回封。
- 曾开板、随后重新一字的对象，仍按当时催化、题材变化和压制关系判断后续机会；首开次数本身不是排除依据。
- 判断顺序：消息细分归类 → 找核心 → 比较题材升级、持平或降级 → 判断次日是否被迫一字 → 判断首开回封是否符合预期。细分范围忠实于实际炒作，前后比较口径一致，股票自身计入数量。
- 一只股票可以属于多个细分。先按催化选定细分，再将完整成员与当日涨停池取交集；成员的其他属性或催化不影响其被计入该交集。
- 同一轮实际消息推动的股票放在一起比较，展示标签的差别通过催化原文厘清。
- 前一日同属性更高身位一字构成压制，作为排除条件；该条件独立于次日高位股低开或低位股竞价更高。
- 竞价卡位指实际同属性、同身位或更高身位对象的竞价高于自身，以开盘涨幅比较。属性关系按催化确认；两只均涨停开盘时视为同一开盘状态，忽略价格取整带来的微小涨幅差。
- 排除并购重组公告驱动的一字票。收购标的或接盘方的行业、业务按公告背景理解，催化性质仍为并购重组。
- 同一天多个对象符合条件，先完成资格判断，再按首次有效首开回封时间排序，只选择最先给出机会的一个。该时间使用对应动作记录。
- 历史案例以当时可见信息判断；分别说明用户给出的数量、研究判断和来源事实，保持起步归类及预期的当时口径。

## 参考

[数据基建](docs/数据基建.md)提供接口字段、单位、来源及维护细节；复盘口径以本文件和用户最新指示为准。
