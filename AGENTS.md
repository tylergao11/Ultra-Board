轻量上阵，不要搞繁重的交付工程，除非我要求
默认不拉子agent；用户明确要求赛马、并行研究或多agent协作时允许
禁止写测试工程
禁止硬编码
真相源统一
接口清晰

## 盲测数据边界

- 个股全部具体题材分类只能使用日期 T 及以前的 `data/kaipanla/raw/`；禁止用同花顺标题、个股事件或人工关键词覆盖开盘啦分类。
- 开盘啦 `theme` 是默认主归属，`raw[12]` / `themes` 是动态归因候选；允许依据故事与资金时序在候选中调整当日交易主属性，但必须保留原始字段和归因证据。
- 同花顺只提供日期 T 及以前的概括故事：读取 `data/ths/stories/` 中标题冒号后的 `story`；标题前半句 `context` 不得作为个股题材。
- 同花顺故事只按可见标题人工核对录入，不建立 OCR 依赖或批处理链路。
- 板数、封板时间、板型与真一字只能使用日期 T 及以前的 `data/ths/limit_pool/`。
- 数据源层禁止输出核心、节点、进攻/防守、评分或买点判断；这些模型等待用户后续重新定义。
- 上午/下午同属性封板数量只描述情绪延续；早封本身不能证明主动性，后继封板本身也不能证明带动关系。
- `.blind-test-quarantine/` 是后验研究与人工标签隔离区；除非用户明确结束盲测并授权事后研究，否则禁止列出、搜索、打开、引用或让程序读取其中内容。
- 任何未来判断入口都必须标注 `information_cutoff`，不得用 T+1 结果或人工标签倒灌。
