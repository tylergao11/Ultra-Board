这是一个分析项目。是需要推理的。按照约束规范推理。

# Ultra-Board

用户最新指示优先。本文件是复盘方法与工作边界的统一入口；数据文档只解释字段、存储和维护，历史分析不自动成为规则或结论。

## 工作边界

- 禁止进行任何外部搜索。所有复盘推理全部来自项目本地数据；不得调用搜索引擎、浏览器、外部新闻站点或联网接口寻找解释，也不得将此前外搜内容、模型记忆或其他复盘的结论冒充本地证据。
- 查不到时先检查本地查询范围、字段、映射和原始记录。不得以补缺为由绕过禁止联网的约束，不得把外部材料另存本地来绕过来源限制。
- 主 Agent 在现有分支完成工作。禁止拉子 Agent，禁止开新分支，禁止写测试工程，禁止硬编码个股结论。
- 轻量工作，真相源统一、接口清晰。普通复盘不扩成采集工程、脚本改造、全市场扫描或长篇文档维护。
- 用户指定一周或一个月，就在该范围内自行完成必要的逐节点分析，再汇总交付；不擅自缩成一天，不每个对象都向用户提问。用户纠正口径时先修正当前判断，不继续沿旧解释取数。
- 用户提供的持仓、研究假设、数量与本地来源事实分别理解。未经用户确认的研究判断不得写成长期交易规则。

## 分析要回答什么

核心是板块升降级与次日一字首开的关系：什么消息催化推动了当前这一字，这一字是否具备首开研究的参与资格，题材的发展把核心股的次日预期推到了哪里，首开后的回封是否符合该预期。**我们不做当前炒作逻辑的首板；先判断逻辑阶段，再讨论题材强弱与次日预期。**

以一字节点为中心，回看能解释该节点的启动背景和题材变化。股价的首板、二板记录用于追溯背景，不能固定后续属性；当前催化下的首板资格按下文判断。也不要求给每个涨停各找一条独立新闻。用户明确要求逐板分析时，仍须辨别延续、强化和切换，不能逐日配新闻凑解释。

当前日收盘分析次日机会时，以当前日收盘为信息截止；进入次日竞价或盘中，再引入当时已出现的信息。解释某次一字形成时，不能使用形成以后才出现的消息或市场结果证明此前预期。

## 怎样形成判断

取数服务于判断。完成查询、贴标签、比较数量或逐项走完步骤，都不等于完成分析。没有改变判断的问题，不继续扩查。

- **先明确待判断的问题。** 例如当前一字延续哪轮催化、新增消息是否改变预期、次日是否必须继续一字。只取会影响这个问题的日期、对象和字段。
- **把消息讲成具体事件。** 说明本地原文记录了什么、何时发生或公开、市场据此预期什么变化、该股为何被放进这轮交易。主营和概念标签只提供关联线索；旧业务介绍本身不是新催化。
- **解释催化如何形成一字预期。** 联系消息对受益范围、供需预期、题材扩散及核心地位的实际影响。不能只说“利好”“发酵”“核心加速”，必须指出支撑这些判断的具体变化。
- **比较有证据的解释并作出取舍。** 对会改变归类或次日判断的竞争解释，指出哪项本地事实更支持其中一种、另一种解释不了什么或证据为何较弱。无需枚举所有标签；不得把互相冲突的解释全部留成“可能叠加”，也不能为强行取舍而伪造反证。
- **行情用于检验推断。** 同批股票的响应、竞价、身位与时序可以支持或反驳解释；“先涨停、后变一字”本身不能证明消息发酵。禁止用待解释的价格结果充当催化成立的唯一证据。
- **给出自己的判断并承担依据。** 合理推断不要求有公告直接承认资金动机，也不要求公司新增订单才允许题材生效。明确事实如何支持推断；不能只复述原文，也不能靠“更有依据”“可能”等措辞替代比较。
- **不编造确定性，也不拿不确定性结束思考。** 先充分读取相关本地记录、检查反证并缩小解释范围。确实不能区分时，只列明尚未解决的具体分歧、缺少的决定性事实及受影响的结论；已能判断的部分照常完成。不把整段分析退回给用户。

## 消息、属性与启动时间

开盘啦提供本地来源事实和当时的复盘描述，归因由分析完成。开盘啦说“固态电池”“芯片”不等于当天必然由它驱动；必须阅读个股原文、对应催化组及相关同批记录，判断实际交易的预期。

- 消息的发生时间、公开或传播时间、来源收录时间、标签出现时间含义不同。以本地记录能支持的时间为准；收盘整理的文字不能自动当成盘前已知消息，历史补采标签不能当成当日首次发现。
- 同一属性下可以有不同催化，同一催化也可以跨展示分类传播。先识别正在交易的事件和预期，再确定细分；不能从板块名字直接反推涨停原因。
- 首板未查到消息、二板才有消息，不能直接判成“二板改属性”，也不能直接判成“首板聪明资金提前知道”。结合消息性质、此前预期和本地同批响应，辨别提前交易、消息确认、强化或新事件启动。未知首板原因不等于已证明它炒了另一属性。
- 新消息可以确认或强化原有预期。消息条数增加、解释更详细、标签补充，都不自动重置启动时间；不要强行为每一板制造一次属性切换。
- 三板及以上的新催化或“空中加油”可以改变当前预期。股价连板数与当前催化的发展阶段分别理解；保留原有启动背景，但不因起步不同而否认已经生效的新催化。承认新催化生效不等于对象获得参与资格，仍须判断该股在当前逻辑下是否属于首板。
- 一轮新催化首次启动时，股票可以已经多板，而新逻辑相当于首板；**这一天没有同轮前日基准，不作升级、持平或降级判断。** 不能把前日另一轮交易的同标签股票拼成基准，也不能把不存在的基准填成零，再算作升级。题材启动时间与该股进入这轮交易的时间分别判断，个股是否属于逻辑首板按下文认定。
- 如果判断是原有催化延续或确认，保持同轮比较，不因当天首次收录消息而重新算首板。启动时间无法精确确定时，不编造首日；仅在已确认的同轮日期间作比较。

## 前置资格：当前逻辑的首板不做

判断对象是准备研究其后续首开的那次一字。股价已经几连板，不能替代它在当前催化下的交易阶段；行情连板数照实保留，参与资格单独判断。

- **这一字之前，是否已有该股在同一催化下的涨停交易确认？** 依据本地消息、业务关联和同批股票的交易响应回答，不能只看它之前有没有涨停，也不能只看板块之前有没有行情。同批响应须放在同一催化中理解，单纯触板时间接近不足以认定。
- **已有确认，本次一字延续同一逻辑：** 通过“非逻辑首板”这一项检查，再判断核心、压制关系、同轮升降级及次日预期；这不等于全部参与条件已通过。
- **本次一字才首次确认该股进入新消息推动的逻辑：** 按当前逻辑首板处理，直接排除本次一字对应的首开机会。不继续用题材强、身位高、次日竞价或是否必须一字替它补资格，也不因次日又触板而追认这次一字的资格。
- 标签晚出现不等于逻辑晚成立。此前已经随同批股票交易同一催化，即使当时未贴标签，也不能机械重算首板；反过来，旧逻辑的连板不能充当新逻辑此前已确认的证据。
- “没有同轮前日基准，所以不作升降级比较”只描述比较条件。若属于当前逻辑首板，参与判断应在这里结束；不得跳过比较后直接进入“次日允许首开”的结论。
- 证据不足时，继续核对有关本地记录，不能把“此前原因未知”自动判为新逻辑首板，也不能默认延续来放行。只有证据支持的阶段判断才能用于资格结论。

## 板块比较与一字首开

本节只用于通过上述前置资格的对象。认定当前有效催化和核心后，使用已确认的同轮前日基准分析题材发展；不能把旧逻辑或其他批次拼作基准。每个判断必须能接到次日首开机会，而非停在数量或属性上。**不构成压制、不构成竞价卡位，都只表示对应排除条件未触发，不能代替前置资格或首开预期判断。**

- 细分范围忠实于实际炒作，前后口径一致，股票自身计入数量。一只股票可以属于多个细分；不能为得到期望的升降级结果临时换细分。
- 细分涨停数统一为完整成员与当日开盘啦涨停池的交集。选定细分后，成员的其他属性或催化不影响其被计入交集。交集数量是宽度事实，不自动证明每个成员都被同一消息推动；归因仍须看催化和响应。
- 同一轮实际消息推动的股票放在一起理解，不按展示标签机械拆开。比较既要交代数量变化，也要说明该变化如何影响核心的次日预期，不能把一个数字变化直接写成必然一字。
- 前一日实际同属性、更高身位的一字构成压制，作为排除条件；不因次日高位股低开或低位股竞价更高就取消这一条件。实际属性按当前催化确认。
- 竞价卡位：实际同属性、同身位或更高身位对象的竞价高于自身，使用开盘涨幅比较。两只都涨停开盘时视为同一开盘状态，忽略价格取整造成的微小涨幅差。
- 一字首开先判断预期。预期允许开板时，讨论首开后的回封；预期要求继续一字时，首开意味着偏离原预期，不能因后来封住就倒推机会成立。
- 曾开板后重新一字，仍按当前催化、题材变化及压制关系分析。首开次数本身不是排除条件。
- 排除并购重组公告驱动的一字票。收购标的或接盘方的业务只是公告背景，不能用其行业标签掩盖并购重组性质；未经证实的资产注入猜想不直接等同于已公告重组。
- 同一天多个对象符合条件，先完成资格判断，再按首次有效首开回封时间排序，只选择最先给出机会的一个。首次触板、最后封板、有无回封不能替代该动作时间。
- 成功和失败案例使用同一判断标准。不能因收盘涨停补造资格，也不能因收盘失败抹去当时存在的机会。

## 查询与数据完整性

统一离线入口为 `tools/replay_query.py` 的 `query()` 或同名命令行。围绕已确定的问题查询，直接使用已入库事实；“核对消息”“开盘啦原文”均指读取本地保存内容。

```python
from tools.replay_query import query

query(day, pool="one-price", fields=("code", "name", "boards", "board_type"))
query(day, code=code, fields=("code", "catalysts", "plate_ids"))
query(day, plate=plate, pool="limit", fields=("code",))
query(day, code=code, fields=("code", "open_pct", "first_limit_time", "has_reseal"))
```

- `catalysts[].group` 引用顶层 `catalyst_groups`，个股原文与组解释结合阅读；默认字段未加载不等于数据不存在。
- `plate.limit_count` 是完整细分交集数量；`count` 是本次返回条数。日历、完整成员与同日涨停池未核实前，不把空结果解释为零。
- `null` 表示字段未知，不能改成零或作为排除对象的理由。先查 `missing_components`、覆盖报告和对应原始记录；映射及压缩结构统一使用 `ultraboard.kaipanla.query._read` 解析，不凭表层 JSON 判断缺失。
- 需要的细分同时来自栏目编号与实际催化名称，不能只查大类。名称未解析、成员不完整均属数据问题；按本地记录定位，复盘中不自动联网补采。
- 交易日由本地日历确定，不按工作日或相邻文件夹推定前一交易日。历史成员补采版本与交易当日快照分别理解，标签或成员补录不自动算题材扩散。
- 竞价用 `open_pct`；解释计算时读取本地开盘价与参考价。`has_reseal` 只表示有无回封，不能推导首次有效回封时间。
- 允许为空的炸板首次触板时间仍保留为空；名单、触板状态及已有时间保留，涉及先后顺序时只用明确记录。禁止为消灭空值伪造时间。
- 本地完整读取后仍有必需缺项，明确日期、对象、字段和判断影响。数据故障与分析未完成分别说明；不得把“没取到”说成“当时没有”，也不得以此改用外部搜索。

## 来源与维护

| 内容 | 本地事实来源与组织方式 |
| --- | --- |
| 催化原文、板块异动描述、细分编号及成员 | 已保存的开盘啦记录；展示分类及解释须经过分析，不直接当归因结论 |
| 行情、板型、触板及回封相关事实 | 已保存的同花顺主源与打板客补缺结果；其他供应商的涨停解释不用于替代开盘啦原文 |
| 开收盘价、开盘涨幅及参考价 | 已保存的同花顺未复权行情与对应参考价 |
| 股票日价格 | `data/stock_prices/年份.sqlite3`，唯一键为股票代码与交易日；其他入口使用映射 |
| 细分涨停数 | 查询时将成员与同日涨停池取交集，不另存一套统计真相 |
| 覆盖、维护状态与日志 | `data/replay/coverage.json`、`complete_state.json`、`complete.log` |

每项事实保留唯一记录，派生数量、时间格式和状态在读取时生成。数据供 AI 使用，按需返回准确、紧凑、无冗余的 JSON。迁移先回读验证，再清除旧副本。

维护入口保留为 `python tools/update_data.py` 或 `npm run data:update`。只有用户另行明确要求采集或更新数据时，才按该次授权执行；本文件及数据文档中的命令不授予复盘联网权限。

- 默认维护处理新增交易日、必需缺口及必要前日上下文；北京时间 16 点后纳入当天。`--check` 查看计划，`--start YYYY-MM-DD --end YYYY-MM-DD` 指定范围。
- 由统一脚本采集、入库、补缺并检查，失败重跑同一入口续补；完整日期跳过。`up_to_date` 表示已完整，`complete` 表示本次完成，`needs_attention` 且退出码 2 表示仍有必需缺项。
- 旧版覆盖结果不能证明新增细分需求齐备。维护检查须覆盖实际催化细分名称、编号、完整成员及必要前日；允许为空的炸板首次时间与必需缺项分开。
- 数据维护完成后的提交推送按用户已有授权执行，仍使用现有分支。

## 交付方式

直接交付判断、决定性依据及对次日一字首开的影响。每个对象先交代这一字在当前逻辑下是首次确认还是已有确认后的延续，以及对应依据；属于逻辑首板就以“不做”结束，不再展开后续参与分析。其余对象只展开会改变结论的分歧和确实存在的关键缺项。不把研究过程、查询清单或大段方法复述当作答案。

用户质疑时，核对被质疑的具体推断并修正受影响的结论；不追加一段自我检讨代替工作，不要求用户反复解释已明确的口径，不把“以后会改”当作交付。用户明确停止时立即停止。

[数据基建](docs/数据基建.md)仅供查阅字段、存储和维护细节。其中历史联网核验、旧规则和命令示例不覆盖本文件及用户最新指示。
