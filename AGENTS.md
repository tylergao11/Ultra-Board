# Ultra-Board

用户最新指示优先。

## 工作方式

- 轻量上阵，不做繁重的交付工程，除非用户要求。
- 禁止子Agent、新分支、测试工程和股票特例硬编码。
- 真相源统一，接口清晰；数据接口只提供来源事实。
- 讨论直接在对话中进行，不自行扩展成代码、统计或文档维护任务。
- 数据面向 AI 复盘：准确、便捷、无冗余。按需返回紧凑 JSON，不为人工可读性重复保存或展开数据。

## 复盘执行顺序

1. 看日期：一次只推进一个交易日，使用已入库的本地数据。
2. 看一字：先查当日一字板，只取代码、名称、身位和必要行情，筛出讨论对象。
3. 看属性消息面：只查这些对象的开盘啦催化及首板、二板起步记录，确定实际细分。
4. 查数量：归类确定后，再查询对应细分在必要日期的涨停交集数量。

- 禁止先收集整月资料、全量催化或所有板块数量再筛选；这个顺序必须实际执行。
- 足以排除的对象当场结束；当天得出判断就直接说明，再推进下一天。

## 本地数据是复盘入口

- 复盘统一使用 `tools/replay_query.py` 的 `query()` 或同名命令行入口。该接口离线读取，不联网，不自动补采。
- 已入库事实直接使用，不以“核实”“交叉验证”“更可靠”为由重新下载、外部搜索或换源比对。
- 下文“开盘啦原文”“核对消息”均指读取本地保存的来源原文；来源名称说明事实出处，不是让 AI 访问对应网站。
- 比较竞价直接请求 `open_pct`；必要时查询本地开盘价和已保存参考价。不能因没有竞价过程快照而认定没有开盘数据。
- 存储文件可能是映射或压缩结构，不得直接用 `json.loads` 把映射当完整行情，也不得因一个目录为空就认定缺数据。底层读取使用 `ultraboard.kaipanla.query._read`。
- 所需字段不在统一查询结果中时，先按[数据基建](docs/数据基建.md)定位对应本地记录，只读取当前对象所需内容。
- 确有缺口时，明确日期、对象、字段及其对判断的影响；不擅自联网、不切换来源、不编造补齐。数据补采属于单独的数据维护任务，仅在用户要求更新或补齐时执行。

### 按需查询

```python
from tools.replay_query import query

# day 为当前交易日；code、plate 为已确定的股票代码和细分编号。
query(day, pool="one-price", fields=("code", "name", "boards", "board_type"))
query(day, code=code, fields=("code", "catalysts", "plate_ids"))
query(day, plate=plate, pool="limit", fields=("code",))
query(day, code=code, fields=("code", "open_pct", "first_limit_time", "has_reseal"))
```

- 命令行同样支持日期、`--code`、`--plate`、`--pool`、`--fields`；字段列表和单位见[数据基建](docs/数据基建.md#ai-离线接口当前合同)。
- `plate.limit_count` 是完整细分成员与同日开盘啦涨停池的交集；`count` 是本次返回股票条数，两者不能混用。
- 催化的 `group` 引用顶层 `catalyst_groups`；读取个股催化时同时看对应组解释，不把组编号当完整消息。
- `null` 表示未知，不得改成零、否或空列表；`missing_components` 表示组件缺失。空股票列表也不能单独证明当天休市。
- 首次触板、最后封板和首次有效回封不是同一个时间；`has_reseal` 仅由开板次数与收盘状态推导，不能据此编造回封时刻。

### 覆盖与已知边界

- 2026-09-15 整理范围为 2024-07-22 至 2026-09-14，共 523 个交易日；后续实际覆盖以 `data/replay/coverage.json` 为准，不能默认范围外也已入库。
- 该范围必需字段缺口为 0。历史炸板首次触板时间有允许的空值：保留炸板名单、触板状态及已取得时间，不再追补，不阻塞复盘；不能据空值比较先后顺序。
- 历史成员关系是供应商补采返回的版本，不等同于交易当日保存的原始快照。数据齐备不代表可以忽略这个时间边界。

## 来源与唯一存储

- 属性、消息催化、板块异动解释、细分成员及涨停交集数量只认开盘啦；不得用财联社、同花顺、打板客的属性或解释替代。
- 行情采用已保存的同花顺主源、打板客补缺结果；开收盘价来自同花顺未复权行情，开盘涨幅使用对应参考价。读取时不重新比对来源或记录差异。
- 以“交易日＋股票代码”组织事实。价格只存 `data/stock_prices/年份.sqlite3`，年行情和每日价格文件只作映射；成员关系只存个股到细分编号，查询时计算交集。
- 催化组解释只存或返回一次，个股引用组编号；禁止按题材重复保存成员行情，也不保存可重算的数量、时间格式和派生状态副本。
- 维护数据时，迁移后回读验证，再删除旧副本；统一补缺入口是 `tools/replay_complete.py`。维护完成按用户授权直接提交推送当前分支，不把维护任务夹进复盘讨论。

## 当前状态

2026-09-14，用户要求“删除当前逻辑，重来”。原有复盘规则、节点结论、持仓路径及方法评价全部停止沿用。

本轮重新确认的讨论口径见下文。旧规则、旧模型和旧结论不因本轮讨论重新生效，不主动从历史聊天或记忆中恢复；当前没有假定持仓。

原始行情、来源事实、查询接口和缓存保留；保留数据不代表保留旧判断。

## 当前核心逻辑：根据消息，细分归类

- 根据开盘啦首板、二板当时的消息，结合启动节点和同批股票的炒作逻辑，认定实际起步属性。公司主营行业、主分类和附带标签只提供线索，不能代替开盘啦催化原文；漏标某个题材，不代表该股没有参与这轮题材炒作。
- 首板、二板都属于起步；三板及以上再新增或切换属性，属于用户不接受的“空中改属性”。不拿后来补上的标签回填起步逻辑。
- 手法是一字首开：找到核心，判断对应题材的发展是否把核心次日的预期推到“被迫继续一字”。没有被迫一字的预期时，首开后的回封是所讨论的机会；若继续一字才合理，首开回封不能按同一种机会处理。
- 不得擅自添加“整轮连板只认第一次开板”的限制。曾开板、随后重新一字的对象，不能仅凭此前开过板排除后续机会；仍按当时的催化、题材变化和压制关系判断。
- 顺序是：消息细分归类 → 找核心 → 比较对应题材的升级、持平或降级 → 判断次日是否被迫一字 → 判断首开回封是否符合预期。先归类，再数涨停；比较口径一致，股票自身计入对应题材的涨停数量。不能靠切出一个与实际炒作不符的小类别，制造“持平、可以做”的结论。
- 同一股票可以属于多个细分。选定催化对应的细分后，按开盘啦完整成员与当日涨停池的交集计数，不得因成员另有属性或催化而手动剔除，也不得先比较各属性数量再挑有利结果倒推催化。
- 只围绕一字首开对象讨论；足以排除的对象当场结束，只补影响判断的缺项，不扩展成无关股票或全量资料研究。
- 同一轮实际消息推动的股票，即使开盘啦展示的标签不同，也必须放在一起比较，不能机械拆开标签漏掉竞争对象。
- 前一日同属性更高身位一字构成压制，不能用次日高位股低开、低位股竞价更高的“反卡”说法撤销这项排除。
- 用户所说的竞价卡位，就是同属性、同身位或更高身位对象“竞价比它高”。不擅自改成封单金额比较，也不额外要求“自身明显走弱、核心地位被破坏”才承认卡位。不凭同地域标签任意建立压制关系，两只都涨停开盘时不把价格取整造成的涨幅差当成卡位。
- 并购重组公告驱动的一字票不做；不能拿收购标的或接盘方的行业、业务，改写成可参与的题材催化。
- 同一天多个对象符合条件，只做最先给出有效首开回封机会的一个。先排除不合格对象，再比较买点时间；不能用最终封板时间替代首次有效回封时间。
- 历史案例按当时能够看到的信息讨论。用户给出的数量、研究判断与独立核验的来源事实分别说明；不把案例判断写成股票特例或自动交易规则，不用后续输赢倒推、改写当时归类和预期。

## 相关文档

- [数据基建](docs/数据基建.md)：本地接口、字段、维护入口与已知限制；其中旧交易讨论不构成当前规则。
- [节点接力复盘接续](docs/节点接力复盘接续.md)：接续记录；与当前指示冲突时以当前指示为准。
