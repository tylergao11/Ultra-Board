# Ultra-Board 唯一分析准则

本文件是本项目唯一的市场分析逻辑真相源。其他文档只允许说明数据、接口和采集边界，不再保存另一套交易模型。若旧记忆、旧结论或旧文档与本文件冲突，一律以本文件为准。

## 一、执行边界

- 轻量分析，不建设繁重的交付、测试或评分工程，除非用户明确要求；
- 禁止调用子 Agent；
- 禁止硬编码方向、阶段、角色、排序和结论；
- 所有判断只对明确的 `trade_date` 和 `information_cutoff` 有效，每次分析必须从截止日前事实重新推导；
- 数据事实与人工判断分开，接口只提供事实，不替代判断；缺失事实保持未知，不用经验补齐；
- 旧结论只能作为待复核假设，不能作为新日期的输入事实；新证据冲突时必须更新结论并说明变化依据；
- 当前默认研究框架是空中接力，不自动切换到低位节点、一进二、冰点、进攻或防守模型。

## 二、核心目标：寻找最容易回流的空中载体

空中接力不是“谁板数最高做谁”，也不是“谁竞价开得最高谁最强”。

研究目标只有两层：

1. 沿着题材从出生至日期 T 的运行轨迹，在现有证据下形成“下一交易日更可能重新获得资金接受”的候选方向；
2. 在候选方向的空中梯队中，预先定义哪只股票在 T+1 做出什么动作才属于超预期，以及板块出现什么响应才支持其带动判断。

“最容易”是同一截止时点下各主要方向的相对比较和条件推演，不是对资金真实意图或未来结果的事实陈述，必须允许被后续盘面证伪。

方向选择与个股选择必须分开：题材资金轨迹用于形成去哪里找的候选方向，个股自身动作和板块反馈共同验证能不能做。竞价只负责验证预案，不能到竞价时才临时发明方向。

## 三、严格的信息截断

分析日期 T 时，只允许读取 T 及以前的盘面、公告和新闻：

- 不读取 T+1 及以后 `/day`、`/stocks`、涨停池、竞价、新闻复盘或结果文件；
- 每个事实必须服从 `information_cutoff` 和 `trade_date`；
- 新闻的实际发生或公开时间必须不晚于 T 收盘；后来的总结文章不能倒灌成当时驱动；
- 已经从对话、记忆或其他渠道知道 T+1 结果时，必须声明“非严格盲测”，不得把推演结果用于证明模型有效；
- 无法让当前会话真正遗忘已经见过的结果，只能隔离证据，不能冒充失忆。

真正验证模型时，应选择结果尚未披露的日期，先固化 T 日判断和 T+1 条件脚本，再揭晓结果。

## 四、题材必须按动态共同资金池归组

来源标签和产业链语义只是候选联系，不是共同资金池或资金因果的证明。共同资金池是针对日期 T 的研究假设：只有截止日前的共同扩散、轮动承接、梯队反馈或日内响应支持时，才暂时合并观察；证据不足时保留为相邻方向，不强行归组。

资金池边界可以随日期拆分、合并或失效。例如算力—通信—PCB—光模块可以在某一阶段共同承担 AI 硬件方向，也可以在另一阶段各自运行；机器人—工业母机—自动化设备同理。不得把示例固化为永久分类。

- 保留股票的全部候选属性，不机械服从单一主标签；
- 多属性股票可能同时连接两个资金池，不能因为标签重叠就断言由谁带动；
- 公告重组、控制权变更、复牌、摘帽等事件驱动必须单独标记，不能用其高度冒充自然题材高度；
- 一字板不因“一字”自动排除。它可以作为板块强度的一项证据，但不能单独证明板块强度，也没有日内交易机会；是否代表自然题材，取决于驱动和板块响应，而不是板型。

## 五、先重建题材的纵向轨迹

每个主要方向都要从出生点开始，逐日还原：

`出生 → 扩散 → 梯队形成 → 分歧淘汰 → 修复或回流 → 过度发酵或衰退`

以上是观察坐标，不是必须依次发生的固定状态机。阶段可以跳过、重复、并存或分化；证据混合时应保留多种解释，不得强塞进单一阶段。

判断必须相对于题材自己的前态，并结合其他主要资金池的同期表现，不能只看 T 日涨停家数。至少回答：

除题材自身的纵向轨迹外，还必须分析同一时段主要板块之间的对抗、轮动与承接关系；只能依据可观察的同步强弱和先后变化提出竞争解释，不能仅因一方上涨、另一方下跌就断言抽血或因果。

当研究对象明确属于高标断板节点时，必须先定位最近一只实际断板的高度股，再从其出生日至断板日追踪候选股票与它的固定身位差、共同属性、同步扩散和资金池归属。T 日某方向梯队完整，只能证明当日截面强，不能自动证明它是新周期：若候选长期固定落后旧高标且共享主要资金池，应先按旧周期补涨或延命解释，除非独立驱动、独立扩散和日内响应能够证明它已经脱离旧池。完成这一步后，优先比较两条承接路径：一是切向低位、寻找刚建立独立扩散的新载体；二是在中位空中梯队中寻找与旧高标资金池分离、具备题材气质和发酵能力的载体。同属性的中位补涨不能仅凭梯队完整或板型强硬获得优先级。

- 最早何时出现有意义的共同扩散，而不是孤立涨停；
- 哪一天建立高、中、低梯队；
- 哪一天扩散过猛，哪些成员在下一日消失、触板失败或继续晋级；
- T 日更接近持续主升、内部淘汰、首次回流、二次回流还是尾盘同层脉冲，是否存在状态并存；
- 可观察盘面更支持持续承接，还是支持离开后重新被接受；证据不足时明确写“无法判断”。

“当前主流”与“最容易回流”不是同一个问题：若盘面没有出现明确的离开—再接受过程，应描述为延续或内部淘汰，不应写成回流。

## 六、主动性与带动性的判定

主动性不是封板早、开得高、缩量、放量、硬板或烂板中的任何单一指标。它是某只股票相对于自身历史、当前板位和题材阶段，率先做出有意义的超预期动作。

主动性必须按相对预期比较，不能横向比较绝对开盘涨幅。先分别写出每只股票基于前一日题材发酵、板型、身位和自身历史形成的正常预期，再比较实际动作偏离正常预期的方向与幅度。绝对开得更高的股票可能低于自身预期，绝对开得稍低的股票也可能明显超出自身预期；不得用“谁开得更高”代替这一判断。

分析日内关系时必须区分：

- 首次触及涨停；
- 炸板；
- 最终回封；
- 随后同题材低位扩散；
- 上方或同层股票是否在它之后回封。

若要把“时间上伴随”升级为“可能带动”，至少需要以下证据链：

1. 候选股票先完成有意义的动作；
2. 相关题材成员随后在时间上连续响应；
3. 响应不只是一只伴随票，而是出现横向扩散或纵向梯队反馈；
4. 头顶股票若此前炸板，是否在该动作之后重新回封；
5. 检查同步新闻、指数共振、另一只股票先启动和多属性切换等竞争解释；无法排除时降低结论强度。

时序只能提供带动证据，不能单独证明因果。若低位先封、上方随后回封，应明确写成“上方可能受到低位买盘帮助”，不能倒写成上方单独带动全场。

## 七、新闻参与形成预期，不替代盘面

新闻必须先判断驱动等级：

- 新发生的硬事件或正式政策；
- 已持续多日的产业趋势；
- 旧闻重复；
- 市场传闻；
- 仅根据股价表现整理的收盘叙事。

新发生的硬驱动通常会抬高先验预期，但影响强弱仍需盘面验证，不能由新闻等级直接推出涨跌。持续多日且已经大面积发酵的逻辑，可能压缩 T+1 的超预期空间，但不能据此直接判定衰退。板块故事只能解释可能交易的属性，不能直接证明某方向打崩、抽血或带动另一方向。

## 八、空中候选与 T+1 脚本

完成题材轨迹后，才选择空中梯队候选。不得按开盘涨幅、板数、封板时间、封单、缩放量或板型单项排序。

正常预期和超预期必须相对于该股票自身历史、当前板位、题材阶段和同期市场环境动态定义，不设跨日期固定阈值。

方向正确、方向核心正确和存在可执行买点是三件事，必须分开记录：

- 题材在 T 日已经完成大范围横向扩散时，会显著抬高其空中载体的 T+1 正常预期，同时压缩继续超预期所需的增量空间；这不自动等于题材次日衰退，但意味着不能把同一轮既有发酵重复计算为新的买入理由；
- 若基于 T 日证据，某只方向核心的正常预期已经是一字锁定、正常情况下无法成交，那么 T+1 继续一字只是符合预期，只承担方向确认作用，不产生盘中买点；
- 上述核心若反而以可成交方式开盘，属于低于原正常预期，在原预案内直接淘汰。此后重新封板只是由低于预期修复回原预期，不能改写成超预期买点；
- “买得到”本身不是优势。最强方向的核心不可交易时，不得机械降级购买同方向低位跟随者，也不得因为另一只股票能够成交就自动把它列为替代品；没有符合条件的可交易载体时，空仓就是执行结果。

最强方向核心无法成交后，其他空中股票可能承接未能进入核心的接力资金，但“资金外溢”只能作为待验证的竞争解释，不能预先写成事实。替代载体必须已经具有独立的抗压或换手检验证据，并在 T+1 相对自身正常预期率先做出主动动作；随后其他股票或对应资金池的连续响应，才支持它从“可交易活口”升级为新的承接核心。它的优势来自主动争夺增量资金，不来自单纯可买。

每只候选必须预先写出：

- T 日它承担的角色，是率先主动、被板块推回封，还是只是高度跟随；
- 结合出生点、历次板型和当日题材阶段，T+1 的正常预期是什么；
- 它在 T+1 是只承担方向确认的不可交易锚，还是可能提供买点的可交易空中载体；
- 怎样开、怎样承接或怎样上板才叫超预期；
- 它完成动作后，板块应出现什么横向发酵或上方回封，才能确认带动；
- 哪些表现会证伪预案。

当候选方向已经由 T 日轨迹建立，且个股已经完成抗压或换手检验时，T+1 个股率先做出的主动超预期动作可以成为空中接力的买入依据。板块响应若客观上只能发生在该买点之后，应作为带动性、持仓逻辑和预案成败的后续验证，不强行伪装成买入前已经知道的事实；执行者是在已有两项证据时博弈第三项，而不是声称三项已经同时成立。候选未主动、后续没有预设响应或竞争解释更强时，预案即被证伪。板块很强不能反推某只股票必须做，股票开得高也不能代替主动性，核心不可交易也不能自动赋予其他股票买入资格。

## 九、动态推演与反错纪律

- 每次更换日期 T，都重新计算资金池边界、题材阶段、个股角色和相对预期，不沿用静态标签、固定排名或上次结论；
- 正文必须区分可追溯的数据事实、基于事实的关系推断、等待 T+1 验证的条件脚本，以及当前无法确认的未知项；
- “回流”“抢筹”“抽血”“带动”等词属于关系推断，不是原始事实；必须给出可观察依据，并同时检查竞争解释；
- 每个关键结论都要写支持证据、反证或失效条件；证据不能区分多种解释时，保留不确定性，不强行选边；
- “最容易”只能在同一信息截止时点比较主要资金池后得出，并写成相对、概率性的判断；
- T+1 结果只用于验证或证伪已固化预案，不得倒改 T 日当时可得的事实和推理。

## 十、标准输出顺序

每次历史复盘按以下顺序展开，不跨步：

1. 声明信息截止日及是否存在未来结果污染；
2. 列出日期 T 前各主要资金池的出生与运行轨迹；若本次研究断板节点，先明确上一实际断板高度及其梯队血缘；
3. 区分持续主流、回流候选和一日脉冲；
4. 还原 T 日日内主动—响应—回封链；
5. 核对驱动的新旧与可靠程度；
6. 分开列出事实、推断、竞争解释与未知项，给出相对更容易回流的方向及反证，但暂不选票；
7. 用户确认后，再写空中候选的 T+1 正常、超预期和证伪脚本；
8. 固化预案后才允许查看 T+1 结果。

## 十一、已经废弃的默认逻辑

以下模型不再作为默认分析入口，也不得混入结论：

- 高标断板日自动套用节点、进攻梯队或防守梯队；
- 退回一字板防守区；
- 从低位节点无限进行一进二、二进三；
- 冰点分数或炸板率直接触发次日买入；
- 无脑最高板；
- 谁竞价开得最高就做谁；
- 用硬板、烂板、缩量、放量或封单单项决定优先级；
- 用静态评分代替题材轨迹与个股动作；
- 仅凭产业链语义就认定属于同一资金池；
- 仅凭一只一字板就证明整个板块强弱；
- 把新闻新旧、先后时序或来源标签直接写成涨跌与带动因果；
- 把生命周期阶段、题材边界或个股角色固化后跨日期复用；
- 读取结果后反推当时应该知道什么。

除非用户明确重新开启某个专题，上述内容仅能作为历史讨论，不得自动调用。

# 附录：Ultra-Board 本地数据查询指南

本文件只说明本地数据的查询入口、命令、参数和字段边界。

## 一、首选入口：本地 Agent API

在仓库根目录执行。该命令直接调用 `site/worker/agent-api.ts`，读取 `site/public/agent-data/v1/`，不需要启动服务器、浏览器或访问公网。

### 1. 检查数据状态

```powershell
npm run api -- "/api/v1/health"
```

只有健康响应中的正式数据状态可用时，才继续查询交易日。

### 2. 查询正式交易日

```powershell
npm run api -- "/api/v1/calendar"
```

只能从响应的 `available_dates` 中选择日期，不得把自然日直接当作交易日。

### 3. 查询一个交易日的完整概览

```powershell
npm run api -- "/api/v1/day?date=2026-07-24"
```

默认返回该日全部来源股票，包括全部首板和更高板，并包含日级市场状态、题材索引、板块故事、股票概览和前后可用交易日指针。

每次 `/day` 只返回一个交易日。需要连续多日时，根据 `navigation.previous_available_date` 或 `navigation.next_available_date` 逐日再次请求，不得假定相邻自然日就是交易日。

### 4. 按题材和板数缩小单日集合

```powershell
# 单一题材的首板
npm run api -- "/api/v1/day?date=2026-07-24&theme=智能电网&board=1"

# 任一题材命中，二板及以上
npm run api -- "/api/v1/day?date=2026-07-24&theme=智能电网&theme=机器人&theme_match=any&min_board=2"

# 同时命中两个题材，二板至四板
npm run api -- "/api/v1/day?date=2026-07-24&theme=AI应用&theme=机器人&theme_match=all&min_board=2&max_board=4"

# 精确查询一板、二板和五板
npm run api -- "/api/v1/day?date=2026-07-24&board=1&board=2&board=5"
```

参数规则：

- `theme` 可重复，最多 8 个；
- `theme_match=any` 表示任一题材命中，`all` 表示全部命中；
- `board` 可重复，最多 16 个，多值取并集；
- `min_board`、`max_board` 均包含边界；
- `board` 不能与 `min_board` 或 `max_board` 同时使用；
- 题材条件与板数条件取交集。

### 5. 批量查询个股完整详情

```powershell
# 按代码批量查询
npm run api -- "/api/v1/stocks?date=2026-07-24&code=600123&code=000001"

# 代码和精确名称可以混用
npm run api -- "/api/v1/stocks?date=2026-07-24&code=600123&name=某某股份"
```

`code` 和 `name` 都可以重复传入，单次最多 32 个唯一选择器。名称只支持精确匹配；名称不唯一时改用代码。

批量详情是原子请求：任一股票未找到、名称不唯一、个股故事缺失或故事正文为空，整批请求失败，不会返回部分结果。

个股详情用于取得默认日概览没有展开的价格、市值、换手、封单、首封、终封、开板次数、板型、连板日期和逐股故事。

## 二、机器发现端点

```powershell
npm run api -- "/api/v1"
npm run api -- "/api/v1/meta"
npm run api -- "/api/v1/guide"
npm run api -- "/api/v1/openapi"
npm run api -- "/api/v1/themes?date=2026-07-24"
npm run api -- "/api/v1/stories?date=2026-07-24"
npm run api -- "/api/v1/news/sources"
```

- `/api/v1`：端点总入口；
- `/meta`：参数上限和查询边界；
- `/guide`：数据调用顺序与完整说明；
- `/openapi`：OpenAPI 3.1 合同；
- `/themes`：指定日期的题材索引；
- `/stories`：指定日期的板块故事；
- `/news/sources`：允许访问的新闻与公告来源。

## 三、Python 单日概览快捷入口

只需要快速查看单日股票和题材时，可以使用：

```powershell
python tools/market_day.py 2026-07-24
python tools/market_day.py 2026-07-24 --theme 智能电网 --board 1
python tools/market_day.py 2026-07-24 --theme AI应用 --theme 机器人 --theme-match all --min-board 2
```

该工具仍遵守单交易日事实接口边界。需要完整个股详情时，改用 `/api/v1/stocks`。

## 四、直接读取正式 JSON

优先使用本地 Agent API。只有检查原始结构或排查接口问题时，才直接读取文件。

```powershell
# 正式发布日期和修订信息
Get-Content -Raw -Encoding UTF8 "site/public/agent-data/v1/manifest.json" | ConvertFrom-Json

# 正式单日组件
Get-Content -Raw -Encoding UTF8 "site/public/agent-data/v1/days/2026-07-24.json" | ConvertFrom-Json

# 开盘啦原始涨停池、梯队和情绪
Get-Content -Raw -Encoding UTF8 "data/kaipanla/raw/2026-07-24/zt_pool.json" | ConvertFrom-Json
Get-Content -Raw -Encoding UTF8 "data/kaipanla/raw/2026-07-24/sector_ladder.json" | ConvertFrom-Json
Get-Content -Raw -Encoding UTF8 "data/kaipanla/raw/2026-07-24/sentiment.json" | ConvertFrom-Json

# 同花顺涨停事实与故事
Get-Content -Raw -Encoding UTF8 "data/ths/limit_pool/2026-07-24.json" | ConvertFrom-Json
Get-Content -Raw -Encoding UTF8 "data/ths/stories/2026-07-24.json" | ConvertFrom-Json
```

正式可用日期以 `manifest.json` 的 `available_dates` 为准。原始目录中存在文件，不代表该日期已经满足正式发布合同。

## 五、字段来源与缺失值

- 个股主属性和候选属性来自开盘啦；
- 板数、价格、市值、换手、封单、首终封、开板次数、板型和真一字来自同花顺涨停池；
- 板块故事和逐股故事来自同花顺故事数据；
- `context` 是来源上下文，不等于个股题材；
- `null` 表示来源未提供或无法确认，不能改写成 `0`、`false` 或“没有”；
- 个股故事缺失时不得用题材、新闻或默认文字补造；
- 所有日期判断以响应中的 `information_cutoff` 和 `trade_date` 为准。

## 六、查询错误处理

- `DATE_NOT_FOUND`：日期未正式发布，回到 `/calendar` 重新选择；
- `STOCK_NOT_FOUND`：至少一个代码或名称未命中；
- `AMBIGUOUS_STOCK_NAME`：名称不唯一，改用股票代码；
- `BOARD_FILTER_CONFLICT`：同时使用了 `board` 和板数范围；
- `STOCK_STORY_INCOMPLETE`：个股故事不完整，不能把本次响应当作完整事实；
- `DATA_CONTRACT_ERROR`：正式组件不符合 Schema，应停止使用该响应。

## 七、详细合同

- `docs/单交易日事实接口契约.md`：端点、参数、响应字段和错误码；
- `docs/每日数据更新契约.md`：数据更新与发布流程；
- `docs/数据基建.md`：数据源、目录职责和完整性边界；
- `site/README.md`：本地 API 调用示例。
